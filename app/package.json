{
  "name": "integration-nation",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc && echo built",
    "lint": "tslint ./tests/**/*.ts && echo linted",
    "test": "tsc && tape ./dist/test/**/*.js",
    "typings": "typings",
    "tap-spec": "tap-spec"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-status": "^0.2.3",
    "supertest": "^2.0.1",
    "tap-parser": "^3.0.3",
    "tape": "^4.6.2",
    "tslint": "^4.0.1",
    "typescript": "^2.0.10",
    "typings": "^2.0.0"
  },
  "dependencies": {
    "tap-spec": "^4.1.1"
  }
}
